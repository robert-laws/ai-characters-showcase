import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://robert-laws.github.io",
  base: "/ai-characters-showcase",
  output: "static",
  trailingSlash: "always"
});
